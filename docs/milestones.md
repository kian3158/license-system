# Milestones (high-level)

0. Repo & dev env (structure + linters + CI skeleton)
1. HW-emulator (dev signing)
2. License Manager skeleton (register/report/heartbeat)
3. Python client (register + report loop)
4. Quota enforcement + disable flow
5. Daily signed summaries (persisted & signed by HSM)
6. SoftHSM integration (PKCS#11) and real HSM migration docs
7. TLS, mTLS, revocation, persistence to Postgres
8. Client packaging (PyInstaller/Nuitka) and production Docker images
9. CI, monitoring, backups, deployment docs
